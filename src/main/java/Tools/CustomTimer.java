package Tools;
import java.util.Timer;

/** The type Custom timer.
 *  This exist solely to prevent the namespace conflict that java cannot handle fom awt.timer
 * */
public class CustomTimer extends Timer {}
