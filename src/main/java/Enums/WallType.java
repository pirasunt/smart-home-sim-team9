package Enums;

/** The enum Wall type. */
public enum WallType {
  /** Windows wall type. */
  WINDOWS,
  /** Wall wall type. */
  WALL,
  /** Door wall type. */
  DOOR,
  /** Outside wall type. */
  OUTSIDE
}
